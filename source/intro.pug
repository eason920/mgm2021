doctype html
html(lang='en')
	head
		meta(charset='UTF-8')
		meta(name='viewport' content='width=device-width, initial-scale=1.0')
		title mgm index
		link(href='./css/index_flat.css' rel='stylesheet')
		link(href='./assets/plugins/perfect-scrollbar-master/perfect-scrollbar.css' rel='stylesheet')
		//-
		script(src='./assets/plugins/jquery/jquery-1.12.4-min.js')
		script(src='./assets/plugins/perfect-scrollbar-master/perfect-scrollbar.min.js')
		script(src='./js/page.js')
		//
			Bootstrap-Core-CSS
			<link rel="stylesheet" href="css/bootstrap.css">
		// Font-Awesome-Icons-CSS
		style.
			/* v scrollbar 是否永遠顥示的跟據，而且一定要放外面，不可寫到 .css 中 v */
			.ps__rail-x,
			.ps__rail-y,
			.ps__thumb-y {
				opacity: .6;
			}
			/* ^ scrollbar 是否永遠顥示的跟據，而且一定要放外面，不可寫到 .css 中 ^ */
		link(href='https://use.fontawesome.com/releases/v5.0.7/css/all.css' rel='stylesheet')
	body
		include header
		.mgmcontent
			.wrapper
				h4.gifttitle.is-first 學習類
				section.giftbox(data-category='1')
					.giftbox-scroller
						include partials/giftbox_item
						include partials/giftbox_item
						include partials/giftbox_item
						include partials/giftbox_item
						include partials/giftbox_item
						include partials/giftbox_item
						include partials/giftbox_item
						include partials/giftbox_item
						include partials/giftbox_item
						include partials/giftbox_item_empty

				h4.gifttitle 生活類
				section.giftbox(data-category='2')
					.giftbox-scroller
						include partials/giftbox_item
						include partials/giftbox_item
						include partials/giftbox_item
						include partials/giftbox_item
						include partials/giftbox_item

				h4.gifttitle 現金類
				section.giftbox(data-category='3')
					.giftbox-scroller
						include partials/giftbox_item
						include partials/giftbox_item
						include partials/giftbox_item
						include partials/giftbox_item_empty
						include partials/giftbox_item_empty

				section.mgmlimit
					p **VIP會員若到期，點數僅保留3個月。
					p **活動有效日期110年8月31日。
					
		include partials/footer

//- ====================================
//- STUDY v
//- option ele 無法加上行為函式 ( 如 @click )
//- data 中的變數可以用 camle case 命名 & 直接 camle  case 引用到 html 中
//- computed 的函式名也可用 camle case 命名、直接 camel case 引用到 html 中
//- var.match(/[?=]/) | var 中有 ? 及 = 時
//- ary = data.filter( item => item.name.split(',')[1] == 'main' );
//- ary.sort(function(n, c){
//- 	if( n.news_id > c.news_id ){ return -1 }else{ return 1 };
//- });
//- 1 維持 , -1 轉
//- ====================================